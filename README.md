# Minimal_Telegram_chat_bot_temperature_sensor
 C# application that interfaces with an Arduino Uno via serial port to collect temperature and humidity sensor data, stores readings in a SQLite database, and integrates with a Telegram bot to reply with the latest sensor reading on demand.
